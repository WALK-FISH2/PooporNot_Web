# 修改记录

状态：Active

## 2026-08-05

- 精简网页版地图工具栏：移除仅面向开发者的“查看配置说明”和低频“查看全部结果”按钮，同时删除无用配置弹窗与事件；地点、厕所、地铁查询完成后的自动视野适配保持不变，工具栏仅保留定位、地铁和昼夜切换。
- 完成网页版全球厕所与地铁支持的文档阶段：确认国内高德/GCJ-02、海外 Leaflet 1.9.4 + Geoapify Tiles/WGS84 的双地图方案，新增 Web 规格、计划、任务、验收和 ADR-0002，并同步架构、配置、安全、部署、测试和发布口径；业务代码尚未修改，真实瓦片 Key 未写入仓库。
- 完成网页版全球厕所与地铁支持代码：新增高德/Leaflet 地图适配层、查询中心与请求淘汰纯逻辑，同源托管 Leaflet 1.9.4；国内保持高德/GCJ-02 和网页步行路线，海外使用 Geoapify Tiles/WGS84 与 Google Maps 外部导航；定位默认查 1 km 厕所，城市切换不查询，文字地点和地图选点采用两步流程，地铁仅由按钮查询 20 km 最近 10 站，厕所与地铁 marker 互相保留。`/api/config` 新增独立浏览器瓦片字段，真实 Key 仅写入被忽略的 `.env`。Node 语法检查、21 项单元测试、差异格式、密钥扫描及 390/768/1440 本地浏览器核心回归通过；真实定位、移动端长按、六城完整 UI、外部导航弹窗和生产配额仍待发布前验证。
- 修复第三方地点服务发生网络错误时，统一后端错误处理因请求路径变量作用域不正确而退出的问题，并增加后端存活回归测试。
- 优化小程序海外文字地点搜索：Geoapify 上游查询自动补充当前城市当地名称以加快中文等多语言消歧；地点搜索使用独立 6 秒超时，成功结果按城市、关键词和数量在进程内缓存 5 分钟（最多 100 组），超时提示与其他供应商故障分开；新增配置、文档和 2 项单元测试。
- 修复小程序切换海外城市后地点搜索提示“海外城市国家代码无效”：查询中心为空时，请求上下文从当前已选海外城市继承 `countryCode`、`cityId` 和 WGS84 坐标系；城市切换后仍保持“先选地点，再查询”的既定流程。
- 小程序在微信开发者工具中跳过真机专用的 `MapContext.moveToLocation` 视口刷新，避免切换地址时反复提示“开发者工具暂时不支持此 API 调试”；开发者工具继续依赖经纬度绑定，真机保留显式刷新。
- 将小程序团队基础库配置从开发者工具已不可选的 `3.13.0` 更新为当前可用的 `3.16.2`，并与本地私有配置保持一致；不再建议选择标记为“灰度中”的 `3.13.2/3.14.3`。
- 修复开发者工具切换海外城市后底图空白：关闭 `project.private.config.json` 中覆盖主配置的自动热重载，避免 `appLaunch with non-empty page stack` 破坏原生地图生命周期；跨城市和查询中心跳转后调用 `MapContext.moveToLocation` 显式刷新视口。
- 修复小程序地图只显示 marker 和腾讯署名、道路底图空白：移除两个同 ID 的条件原生 `<map>`，恢复为单一常驻默认地图实例；未配置腾讯个性化样式时不再传 `subkey` 或 `layer-style`。
- 优化国内启动定位：小程序先获取 GCJ-02，位于中国大陆候选范围时直接调用高德逆地理并进入国内链路；仅海外位置补取 WGS84 并调用 Geoapify，国内正常启动不再依赖海外服务。
- 优化海外 POI 性能：厕所和地铁由“公共 Overpass 主请求、Geoapify 兜底”改为直接调用 Geoapify Places，删除活动链路和配置中的 Overpass，避免最长约 8 秒的串行超时等待。
- 海外厕所上限统一为最近 100 个，海外地铁仍为 20 km 内最近 10 站；Geoapify 默认超时改为 `GEOAPIFY_TIMEOUT_MS=4000`。
- 更新供应商单元测试、API 类型、架构、配置、规格、任务、验收、部署、安全隐私和开发历史文档；Node 语法检查、14 个单元测试和小程序 TypeScript 检查通过。
- 独立端口真实网络冒烟：无锡高德逆地理约 186 ms；新加坡 Geoapify 厕所约 1.45 s、地铁约 0.94 s。该单次结果不作为生产 SLA，六城多轮与真机仍待复测。
- 文档区分当前局域网测试 API 地址和 `https://pp.nuanzhualife.cn` 生产目标；本轮未覆盖用户当前的局域网地址配置。
- 修复本地定位时全球逆地理返回 `502` 的配置问题：本地开发验收恢复使用已验证的 Geoapify 开发项目 Key，生产 Key 延后到服务器部署前切换。
- 修复腾讯地图 `style0` 警告：未配置 subkey 和深浅样式 ID 时渲染不带 `subkey`、`layer-style` 属性的地图；只有三项配置齐全时才启用个性化底图。
- 修复微信开发者工具预览 `pages/index/index.js Unexpected token: punc (.)`：移除全球地图选点代码中的可选链语法，兼容项目当前微信编译与压缩链；不改变查询中心和国家代码继承逻辑。
- 完成微信小程序“全球厕所与地铁支持”代码实现：首次 WGS84 定位识别国家，中国大陆继续使用高德/GCJ-02，海外使用 Overpass/Geoapify 与 WGS84。
- 增加“中国大陆/其他地区”、六座重点海外城市、当前城市硬边界地点搜索、地图长按临时点与“选这里”；文字地点和地图选点均保留“先选定、再点击查厕所”的两步流程。
- 查询中心、地图视口和临时点改为显式状态；城市切换只移动视口并清空查询中心，没有中心时查询按钮和半径变化不发请求。
- 厕所和地铁改为独立请求与 marker 图层：半径只刷新厕所；地铁只由专用按钮查询 20 km 内最近 10 站；同一查询中心下两类结果互相保留。
- 国内地铁后端改为一次高德周边查询并匹配 `data/metro` 厕所状态，取消活动链路中的自动预加载、逐城分页坐标索引和多城市冷扫描；旧国内接口路径和未传海外参数的调用保持兼容。
- 新增 Geoapify、Overpass、HTTP、供应商错误和 POI 规范化模块，支持 node/way/relation、距离排序、地铁去重、厕所 200/100 上限、超时/429/网络/5xx/非法响应降级，以及安全诊断日志。
- 增加 `data/global/cities.json`、14 个 Node 单元测试和小程序坐标工具；扩展 `npm run check`，加入 `npm test` 和 TypeScript 类型检查说明。
- `.env.example` 改为无密钥占位模板并纳入版本控制；本机被忽略的 `.env` 在本地验收期间使用开发 Geoapify 项目 Key，真实值未写入文档或客户端。
- 六座重点城市后端链路和无锡国内接口已冒烟通过；微信 Android/iOS 真机长按、marker、`wx.openLocation` 与外部地图落点仍待验收。Android 构建因本机 JDK 14、项目要求 JDK 17而未完成。
- 同步更新 README、AGENTS、架构、API、配置、地铁数据、开发、部署、测试、发布、安全隐私、路线图、开发历史、功能规格、任务和验收文档。
- 完成“海外厕所与地铁支持”实施前冲突确认，并同步修订功能规格、实施计划、任务清单、验收标准和 ADR。
- 确认文字地点与地图长按选点统一采用“先选定，再点击查厕所”的两步流程；首次定位和已有查询中心时切换半径仍可自动查询厕所。
- 确认城市切换只移动地图并清空正式查询中心；未选择地点时，查询按钮和半径变化不发请求。
- 确认地铁取消自动预加载，只在点击专用按钮后查询当前中心 20 km 内最近 10 个站点，并保留已有厕所 marker。
- 本次只修改待实施需求文档和修改记录，没有修改业务代码、运行配置、现状型 Active 文档或地铁 JSON。

## 2026-08-04

- 建立正式中文开发文档体系：产品规格、架构、API、配置、地铁数据、开发历史、安全隐私、开发、部署、测试、发布、路线图、ADR 和功能规格模板。
- 重写根目录、微信小程序、Android 和地铁数据 README，使其与当前代码、默认端口、HTTPS 地址和三端导航差异一致。
- 新增 `AGENTS.md`，固定事实来源、平台边界、数据规则、安全要求、验证流程和每次修改必须追加本文件的约定。
- 记录当前地铁数据盘点：52 个城市索引、5 个已有数据城市、21 个线路文件、408 条站点记录。
- 本次只修改开发文档，没有修改业务代码、运行配置或地铁 JSON。

## 2026-08-02

- 将“不确定是否有厕所”的地铁站状态统一为橙色（`#F59E0B`）。
- 网页端和微信小程序端沿用现有橙色显示；补齐 Android 端的橙色地图图标与结果列表状态点。
- 微信小程序移除厕所和地铁站的“路线”按钮及路线绘制界面，仅保留调用微信地图位置页的“导航”。
- 微信小程序停止调用后端 `/api/navigation` 接口；网页版路线功能保持不变。
- 后续代码修改统一记录在本文件中。
